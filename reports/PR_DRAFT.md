PR Draft: dev/sprint-5-payment-adapter → main (draft)

Date: 2026-07-07
Branch: dev/sprint-5-payment-adapter (pushed to origin)

Summary
-------
This PR consolidates the payment-adapter work completed in Sprint 5:
- Adds a pluggable payment-adapter compatibility shim
- Implements a mock/stripe-mock adapter for local/dev testing
- Adds feature flags to gate live payments (ENABLE_PAYMENTS, ENABLE_LIVE_DEVNET)
- Includes unit tests and compatibility shims to allow future adapter additions

Verification
------------
Local verification performed before drafting this PR:
- npm run build: success
- npm test (vitest): all unit tests passed (recent runs: 241 tests passed locally)
- Unit tests intentionally exercise mocks for external services (Jupiter, Poster); warnings are expected and non-blocking

Notable constraints & safety
---------------------------
- Payments are feature-flagged and disabled by default. Real trading is restricted to devnet only per PROJECT_SPEC.md.
- Do NOT add BOT_TOKEN, BOSS_CHAT_ID, Helius keys, or other secrets into the repo or committed .env files. The assistant will not add secrets.
- Telegram sending, opening PRs, spawning external coding agents (Claude), and running DeepSeek must be explicitly authorized with exact phrases (see "Next steps").

Files / Areas touched (high-level)
---------------------------------
- payment adapter modules and compatibility shims
- tests covering payment adapter behavior and mocks
- reports/ and drafts/ updated with sprint artifacts

PR body (suggested)
-------------------
Title: feat(payment): payment adapter compatibility shim + mock adapter (Sprint 5)

Body:
This PR introduces the payment adapter compatibility shim and a mock adapter for local/dev testing as part of Sprint 5.

Key points:
- Payment functionality is feature-flagged (ENABLE_PAYMENTS, ENABLE_LIVE_DEVNET). Default configuration disables live payments.
- Unit tests cover the adapter shim and mocked flows; tests pass locally (241 tests).
- External integrations (Stripe/Jupiter/Poster) are mocked in tests; any runtime network calls should be gated behind the feature flags.

Testing performed:
- npm run build
- npm test (vitest) — all tests passing locally

How to review:
- Focus review on payment adapter API surface (adapter interface, shim behavior, and feature-flag gating)
- Confirm no secrets or credentials are checked in
- Check tests for adequate coverage of success/failure/mocked external errors

Suggested reviewers:
- @<reviewer1> (payments)
- @<reviewer2> (backend)
(Replace with actual GitHub handles when creating the PR)

Commands to open PR (do NOT run automatically without explicit authorization):
- cd /Users/m4/Web3_AI_Agency/projects/copy-trade-bot
- git fetch origin --prune
- git checkout dev/sprint-5-payment-adapter
- git pull --rebase origin dev/sprint-5-payment-adapter
- npm --prefix . run build && npm --prefix . run test
- gh pr create --base main --head dev/sprint-5-payment-adapter --title "feat(payment): payment adapter compatibility shim + mock adapter (Sprint 5)" --body "./reports/PR_DRAFT.md"

Next steps / Exact authorization phrases
---------------------------------------
- To open a PR to main: reply exactly: allow open-pr to main
- To open a PR to dev: reply exactly: allow open-pr to dev
- To send the Telegram sprint summary (assistant will use latest draft in drafts/): add BOT_TOKEN and BOSS_CHAT_ID to project-level or workspace-level .env, then reply exactly: allow send
- To continue coding with Claude CLI: reply exactly: authorize spawn claude
- To run DeepSeek tests after Claude: reply exactly: authorize spawn deepseek/deepseek-chat
- To run e2e/devnet demo: populate DEVNET_RPC and Helius keys in .env and reply exactly: allow e2e devnet

Notes
-----
- A record of all automated draft Telegram attempts is stored under reports/TELEGRAM_ATTEMPT_*.md and will show NOT_SENT until credentials are provided.
- If you want the assistant to stand down and return HEARTBEAT_OK on the next heartbeat, reply exactly: stand down

---
Generated by assistant (draft only). No external actions performed.
