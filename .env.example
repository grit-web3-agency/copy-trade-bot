# Telegram Bot
BOT_TOKEN=your_telegram_bot_token_here

# Solana RPC (Helius free tier)
HELIUS_API_KEY=your_helius_api_key_here
SOLANA_RPC_URL=https://devnet.helius-rpc.com/?api-key=YOUR_KEY
SOLANA_WS_URL=wss://devnet.helius-rpc.com/?api-key=YOUR_KEY

# Devnet RPC (used when user mode=devnet; must be a devnet endpoint)
DEVNET_RPC=https://api.devnet.solana.com

# Network (devnet only for MVP)
SOLANA_NETWORK=devnet

# Live devnet trading (set to 'true' to enable real on-chain swaps on devnet)
# Default: false (dry-run mode — quotes only, no transactions submitted)
ENABLE_LIVE_DEVNET=false

# Copy Policy Defaults
DEFAULT_MAX_TRADE_SIZE_SOL=0.1
DEFAULT_SLIPPAGE_BPS=100

# Payment / Subscription (devnet only, mocked verification)
TREASURY_WALLET=your_treasury_wallet_pubkey_here

# Poster / Activity Logging
DASHBOARD_API_URL=http://127.0.0.1:8088
DISCORD_WEBHOOK_URL=
POSTER_ENABLED=true
